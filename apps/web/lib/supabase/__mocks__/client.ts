export const createSupabaseBrowserClient = jest.fn().mockReturnValue({
  // The browser client needs no specific mock implementations for our current tests.
  // We just need the module to exist and export the function.
});
